{
  "name": "@energetika/db",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "seed": "bun run src/seed.ts",
    "studio": "prisma studio",
    "demo": "bun run src/demo-content.ts",
    "test-content": "bun run src/test-content.ts",
    "test-content:clean": "bun run src/test-content-clean.ts"
  },
  "dependencies": {
    "@energetika/shared": "*",
    "@prisma/client": "^6.2.0",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "prisma": "^6.2.0",
    "typescript": "^5.7.2"
  }
}
